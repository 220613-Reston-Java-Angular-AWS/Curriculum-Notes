
export function compute(number: number){
    
    //note that we have 2 cases to test
    // where a number is negative & where a number is positive
    if(number<0)
         return 0;


    return number + 1;
}